plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.4.0'
    id "ru.endlesscode.bukkitgradle" version "0.8.2"
    id 'com.github.johnrengelman.shadow' version '2.0.4'
    id 'maven-publish'
    id "org.hibernate.build.maven-repo-auth" version "3.0.4"
}

group 'com.github.darkvanityoflight.darkmodcore'
version '1.0-SNAPSHOT'
description 'The base plugin for all of my Minecraft plugins'

repositories {
    mavenCentral()
    maven { url 'https://repo.repsy.io/mvn/darkvanityoflight/darkmods' }
    spigot()
}

dependencies {
    compileOnly spigotApi()
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib"
}

bukkit {
    version = "1.16.3"

    meta {
        name = "DarkModCore"
        description = 'The base plugin for all of my Minecraft plugins'
        main = "com.github.darkvanityoflight.darkmodcore.DarkModCore"
        version = version
        authors = ["DarkVanityOfLight"]
    }

}

publishing {
    publications {
        DarkModCore(MavenPublication) {
            groupId = 'com.github.darkvanityoflight'
            artifactId = rootProject.name
            version = this.version

            from components.java
        }
    }
    repositories {
        maven {
            name = 'DarkMods'
            url 'https://repo.repsy.io/mvn/darkvanityoflight/darkmods'
        }
    }
}